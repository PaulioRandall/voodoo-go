package farm

/*
import (
	"testing"

	"github.com/PaulioRandall/voodoo-go/parser_2/scantok"
	"github.com/PaulioRandall/voodoo-go/parser_2/token"
	//"github.com/PaulioRandall/voodoo-go/parser_2/utils"

	"github.com/stretchr/testify/require"
)

func doTestFarm_Harvest() {

	last := SaltHarvest()
	require.NotNil(t, last, `Farm.SaltHarvest() != nil`)
	if len(last) != 0 {
		act = append(act, h)
	}

	assertStatements(t, exp, act)
}

func assertStatements(t *testing.T, exp, act [][]token.Token) {
	for i, _ := range exp {
		require.True(t, i < len(act), `Statement[%d] missing`, i)
		scantok.AssertSliceEqual(t, exp[i], act[i])
	}

	assert.Equal(t, len(exp), len(act), `len(exp) == len(act)`)
}
*/
